run = "cd backend && mvn clean package -DskipTests && java -jar target/png-steganography-1.0.0.jar"

[nix]
channel = "stable-22_11"

[nix.dependencies]
pkgs = ["openjdk17", "maven"]

[deployment]
run = ["sh", "-c", "cd backend && mvn clean package -DskipTests && java -jar target/png-steganography-1.0.0.jar"]

[[ports]]
localPort = 8080
externalPort = 80
